<div class="">
    <div>
        <img src="{{item.img}}" data-edit="img" title="{{item.name}}" class="" height="64" width="64">
    </div>
    <div>
        <input id="em_item_name" class="" name="" type="text" value="{{item.name}}" placeholder="name">
    </div>
    <div>
        <input id="em_item_price" class="" name="price" type="number" value="{{item.system.price.value}}">
        <i class="fa-solid fa-coins"></i>
    </div>
</div>